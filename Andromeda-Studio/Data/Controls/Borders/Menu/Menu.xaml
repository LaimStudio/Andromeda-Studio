<UserControl x:Class="AndromedaStudio.Data.Controls.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AndromedaStudio.Data.Controls">
    <UserControl.Resources>
        <Style TargetType="Frame">
            <Setter Property="NavigationUIVisibility" Value="Hidden"/>
        </Style>
    </UserControl.Resources>
    <Border HorizontalAlignment="Center" VerticalAlignment="Center" Height="380" Width="270"
            Effect="{StaticResource Shadow2}" Background="{DynamicResource Color1}" CornerRadius="3">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="5"/>
            </Grid.RowDefinitions>
            <Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource Color2}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label x:Name="PageName"
                           Foreground="{DynamicResource TextColor2}" FontFamily="Segoe UI"
                           Margin="3,0,0,0" VerticalContentAlignment="Center" Grid.Column="1"/>
                    <local:TextButton Icon="{StaticResource ArrowLeftIcon}" Click="Back"
                                          Margin="5"
                                          IconSize="12"
                                          IconFill="{DynamicResource Color7}"
                                          HorizontalAlignment="Right"/>
                </Grid>
            </Border>
            <Grid x:Name="Frames" Grid.Row="1" ClipToBounds="True">
                <Frame x:Name="Frame" Margin="13,0,13,0"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
